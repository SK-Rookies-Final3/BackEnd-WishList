package com.wishlist.util;

public class HeaderUtils {

    // HTTP 요청 헤더에서 userId를 추출하는 유틸리티 메서드 (이제는 @RequestHeader 사용)
    public static String getUserIdFromHeaders(String userId) {
        return userId;
    }
}
